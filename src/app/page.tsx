"use client";

import React, { FC } from "react";
import Section from "@/components/Section";

const Home: FC = () => {
  return <Section heading="Home Section">HomePage</Section>;
};

export default Home;
